# Added by Payload
PREVIEW_SECRET=YOUR_SECRET_HERE
# Used to validate preview requests
CRON_SECRET=YOUR_CRON_SECRET_HERE
# Secret used to authenticate cron jobs
NEXT_PUBLIC_SERVER_URL=http://localhost:3000
# Used to configure CORS, format links and more. No trailing slash
PAYLOAD_SECRET=65dfa958a74d47d9a10bb595
# Used to encrypt JWT tokens
#DATABASE_URI=postgresql://127.0.0.1:5432/your-database-name
# Or use a PG connection string
DATABASE_URI=postgres://postgres:postgres@127.0.0.1:5432/gutscheineland
# Database connection string

# Email Configuration (Brevo SMTP)
SMTP_HOST=smtp-relay.brevo.com
# SMTP server hostname (Brevo: smtp-relay.brevo.com)
SMTP_PORT=587
# SMTP server port (587 for TLS, 465 for SSL)
SMTP_SECURE=false
# Use SSL/TLS (true for port 465, false for port 587)
SMTP_USER=YOUR_BREVO_SMTP_USER
# SMTP username provided by Brevo
SMTP_PASS=YOUR_BREVO_SMTP_PASSWORD
# SMTP password provided by Brevo
SMTP_FROM=no-reply@gutscheineland.de
# Default sender email address